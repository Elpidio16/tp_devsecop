pipeline {
        agent none
        stages {
         
          stage("build & sonarqube_scanner") {
            agent any
            steps {
              withSonarQubeEnv('sonarqube_scanner') {
                sh 'mvn clean package sonar:sonar'
              }
            }
          }
        }
      }


